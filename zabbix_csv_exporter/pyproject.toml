[project]
name = "zabbix-csv-exporter"
version = "0.0.1"
description = "Dify tool plugin that exports Zabbix inventory as CSV files"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin>=0.9.0",
]

[dependency-groups]
dev = []

